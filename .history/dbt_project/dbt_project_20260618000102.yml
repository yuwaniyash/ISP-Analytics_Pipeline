name: 'isp_project'
version: '1.0.0'
profile: 'isp_project'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target"]

models:
  isp_project:
    staging:
      +materialized: view
    dimensions:
      +materialized: table
    facts:
      +materialized: table
    marts:
      +materialized: table